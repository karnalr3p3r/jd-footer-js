## Jacob's First JS Footer Module

> This should be used in the following manner

Install with the command:

```
npm install --save firstNPMmodule
```

Add to project with following code:

```javascript
import { footer } from 'firstNPMmodule';

footer('some name')
```